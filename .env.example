VITE_SUPABASE_URL=https://nkceyzovrtmiusamobsf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here
VITE_ADMIN_EMAILS=admin@example.com
# Public URL of the secure server endpoint that creates Stripe Checkout Sessions.
# Leave empty while card payments use the local test-card flow.
VITE_STRIPE_CHECKOUT_ENDPOINT=
# Public HTTPS URL of the protected Edge Function that creates manual Auth users.
# Leave empty until the server-side role checks, profile writes and audit log are ready.
VITE_ADMIN_USER_ENDPOINT=
